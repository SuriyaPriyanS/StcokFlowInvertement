apiVersion: apps/v1
kind: Deployment
metadata:
  name: stockflow-redis
  namespace: stockflow
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: stockflow-redis
  namespace: stockflow
spec:
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
